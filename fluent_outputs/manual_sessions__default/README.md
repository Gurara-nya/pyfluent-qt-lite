# manual_sessions__default

Default manual Fluent workspace for PyFluent Lite.

The desktop app uses `workspace/` here when no project-specific working
directory has been selected. For campaign work, prefer creating a dedicated
project folder under `fluent_outputs/` and using that project's `workspace/`.
